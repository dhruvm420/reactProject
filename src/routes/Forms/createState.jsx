import RegistrationForm from "../../components/registrationform";
import Root from "../root";
export default function CreateState() {
  return (
    <Root title="State Form">
      <RegistrationForm title="state" />
    </Root>
  );
}
